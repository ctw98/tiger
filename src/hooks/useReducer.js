import React,{useReducer} from "react";

//useReducer用于在函数组件中管理复杂的状态。
//它接受一个reducer函数和一个初始状态，并返回一个包含当前状态和一个派发操作的数组。派发操作将一个操作对象发送到
//reducer函数中，并返回一个新的状态

const initialState = {count:0};

function reducer(state,action){
    switch (action.type) {
        case '':
            
            break;
    
        default:
            break;
    }
}